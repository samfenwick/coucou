[project]
name = "subcurrent"
version = "0.1.0"
description = "Real-time audio captioning with synced subtitles"
requires-python = ">=3.10"
dependencies = [
    "websockets>=12.0,<14.0",
    "numpy>=1.24,<3.0",
    "httpx>=0.27,<1.0",
]

[tool.uv]
exclude-newer = "7 days"
